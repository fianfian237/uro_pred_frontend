#setup:
# addons:
#   - plan: heroku-postgresql
build:
 docker:
   web: Dockerfile
# config:
#   REQUIREMENTS_FILENAME: heroku.yml
#release:
# image: uro_pred_backend
# command:
#   - ./release.sh
run:
 web: gunicorn -w 1 -b 0.0.0.0:8050 app:server